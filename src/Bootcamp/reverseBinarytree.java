package Bootcamp;

import java.util.Scanner;

//트리를 배열로 생각하고 리버스 규칙에따라 인텍스 안의 밸류를 바꿔준다
public class reverseBinarytree{

    public static void main(String[] args) {

        int[] input = {4,2,7,1,3,6,9};//입력되는 값(트리의 각 노드 값
        int expo = 0;//배열이 2의 거듭제곱 단위로 짤림
        int square = Integer.parseInt(String.valueOf(Math.round(Math.pow(2,expo))));//배열을 자르기위한 키
        for(; square < input.length; expo++){

        }
    }

}